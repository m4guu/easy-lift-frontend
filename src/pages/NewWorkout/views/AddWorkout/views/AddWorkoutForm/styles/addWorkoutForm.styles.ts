import { Box, Typography } from "@mui/material";
import { styled } from "@mui/system";

export const FieldWrapper = styled(Box)({});

export const ExerciseTitle = styled(Typography)({});

export const SetsContainer = styled(Box)({});
