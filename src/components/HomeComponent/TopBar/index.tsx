import React from "react";

import { styled } from "@mui/system";

const TopBar: React.FC = () => {
  return <TopBarSection>TOP BAR</TopBarSection>;
};

const TopBarSection = styled("section")({
  height: "100%",
});
export default TopBar;
